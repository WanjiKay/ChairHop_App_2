<nav class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid header-display-desktop" data-content-field="site-title">

    <!-- Logo / Site title -->
    <div class="header-title-nav-wrapper">
      <div class="header-title" data-animation-role="header-element">
        <div class="header-title-text">
          <%= link_to root_path, id: "site-title", data: { animation_role: "header-element" } do %>
            <%= image_tag "logo.png", alt: "ChairHop Logo", class: "navbar-logo" %>
          <% end %>
        </div>
      </div>

      <!-- Navigation links -->
      <div class="header-nav">
        <div class="header-nav-wrapper">
          <nav class="header-nav-list">
            <div class="header-nav-item header-nav-item--collection">

            <div class="header-nav-item header-nav-item--collection">

            </div>
            <div class="header-nav-item header-nav-item--collection">

            </div>
          </nav>
        </div>
      </div>
    </div>

    <!-- Right-side actions -->
    <div class="header-actions header-actions--right">
      <% if user_signed_in? %>
        <div class="dropdown header-actions-action">
          <a class="avatar-dropdown-toggle"
            id="navbarDropdown"
            aria-expand="false">
            <% if current_user&.avatar&.attached? %>
              <%= image_tag url_for(current_user.avatar), class: "avatar-img", alt: "User menu" %>
            <% else %>
              <%= image_tag "profile.png", class: "avatar-img", alt: "User menu" %>
          <% end %>
          </a>

          <div class="dropdown-menu dropdown-menue-end" arial-labelledby="navbarDropdown">
            <% link_to "Profile", profile_path(edit: 1), class: "dropdown-item" %>
            <% link_to "Settings", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Chats", chats_path, class: "dropdown-item" %>
            <%= link_to "Log out", destroy_user_session_path, data: { turno_method: :delete}, class: "dropdown-item" %>
          </div>
        </div>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "btn btn-outline-light ms-3"%>
      <% end %>
    </div>
  </div>

</nav>
<style>
  .navbar-logo { height: 40px; width: auto; }
</style>
