<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; color: #333; max-width: 600px; margin: 0 auto; }
    .header { background: #6c757d; color: white; padding: 24px; text-align: center; }
    .header h2 { margin: 0; }
    .body { padding: 24px; }
    .detail-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #eee; font-size: 14px; }
    .detail-row:last-child { border-bottom: none; }
    .card { border: 1px solid #dee2e6; border-radius: 6px; padding: 16px; margin: 16px 0; }
    .alert-warning { background: #fff3cd; border: 1px solid #ffeeba; border-radius: 6px; padding: 12px 16px; margin: 16px 0; }
    .btn { display: inline-block; background: #212529; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: bold; margin-top: 16px; }
    .footer { background: #f8f9fa; padding: 16px; text-align: center; font-size: 12px; color: #6c757d; margin-top: 24px; }
  </style>
</head>
<body>
  <div class="header">
    <h2>Booking Update</h2>
  </div>
  <div class="body">
    <p>Hi <%= @customer.full_name %>,</p>
    <p>Unfortunately, <strong><%= @stylist.full_name %></strong> is unable to accept your booking request.</p>

    <% if @appointment.payment_amount.present? %>
      <div class="alert-warning">
        Your deposit of <strong><%= number_to_currency(@appointment.payment_amount.to_f * 0.5) %></strong> will be refunded within 5&ndash;10 business days.
      </div>
    <% end %>

    <div class="card">
      <div class="detail-row"><span>Stylist</span><strong><%= @stylist.full_name %></strong></div>
      <div class="detail-row"><span>Service</span><strong><%= @appointment.selected_service %></strong></div>
      <div class="detail-row"><span>Date &amp; Time</span><strong><%= format_datetime(@appointment.time) %></strong></div>
      <div class="detail-row"><span>Booking #</span><strong>#<%= @appointment.id.to_s.rjust(6, "0") %></strong></div>
    </div>

    <p>You can find another stylist on ChairHop.</p>
    <a href="<%= stylists_url %>" class="btn">Find Another Stylist</a>
  </div>
  <div class="footer">&copy; ChairHop &middot; You're receiving this because you have an account with us.</div>
</body>
</html>
